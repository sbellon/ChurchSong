name: Python Code Quality
on:
  push:
    paths-ignore:
      - '.github/**'
  pull_request:
    branches:
      - main
env:
  UV_LOCKED: 1
jobs:
  lockfile:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/setup
      - name: Check lockfile
        run: uv lock --check
  linting:
    runs-on: windows-latest
    needs: [lockfile]
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/setup
      - name: Run ruff check
        run: uvx ruff check .
  formatting:
    runs-on: windows-latest
    needs: [lockfile]
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/setup
      - name: Run ruff format
        run: uvx ruff format --check .
  typing:
    runs-on: windows-latest
    needs: [lockfile]
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/setup
      - name: Sync dependencies
        run: uv sync --all-extras --dev
      - name: Run pyright
        run: uvx pyright --project .
  testing:
    runs-on: windows-latest
    needs: [lockfile]
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/setup
      - name: Sync dependencies
        run: uv sync --all-extras --dev
      - name: Run pytest
        run: uv run pytest
  typos:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/setup
      - name: Sync dependencies
        run: uv sync --all-extras --dev
      - name: Run typos
        run: uvx typos --force-exclude
  build:
    runs-on: windows-latest
    needs: [linting, formatting, typing, testing, typos]
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/setup
      - name: Run build
        run: uv build
